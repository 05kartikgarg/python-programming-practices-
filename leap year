#check if leap year
n=int(input("enter the year\n"))
if n%4==0:
	if n%100==0:
		if n%400==0:
			print("leap year")
		else:
			print("non leap year")
	else:
		print(" leap year")
else:
	print("non leap year")
